/**
 * Estilos do Design System
 *
 * Este módulo exporta os estilos do Design System.
 * Os estilos são bundlados em dist/styles.css durante o build.
 *
 * Para usar os estilos, importe o arquivo CSS:
 *
 * @example
 * ```tsx
 * import '@carloshb/ds/styles.css';
 * ```
 *
 * Ou via CDN:
 * ```html
 * <link rel="stylesheet" href="https://unpkg.com/@carloshb/ds@latest/dist/styles.css" />
 * ```
 */

// Este arquivo é apenas para documentação.
// Os estilos reais estão em src/styles/bundle.css e são processados pelo Vite.
// O bundle final é gerado em dist/styles.css durante o build.

export {};
